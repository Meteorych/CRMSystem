@model System.Collections.Generic.List<WebApplication1.Data.Models.ApplicationUser>


@{
    ViewBag.Title = "Пользователи";
}

<h2>Пользователи</h2>


<table class="table table-bordered table-striped">
    <tr>
        <th>Имя пользователя</th>
        <th>Роль пользователя</th>
    </tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.UserName</td>
            <td>
                <form asp-action="SetUserRole" method="post">
                    <div class="row align-items-center">
                        <div class="col">
                            <select name="role" class="form-control">
                                @foreach (var role in ViewBag.Roles)
                                {
                                    @if (user.UserRoles.Any(ur => ur.Role.Name == role.Name))
                                    {
                                        <option value="@role.Name" selected>@role.Name</option>
                                    }
                                    else
                                    {
                                        <option value="@role.Name">@role.Name</option>
                                    }
                                }   
                            </select>
                        </div>
                        <div class="col-auto">
                            <button type="submit" class="btn btn-primary">Изменить роль</button>
                        </div>
                    </div>
                </form>
            </td>
        </tr>
    }
</table>